# Per-date baselines

One folder per date (YYYY-MM-DD). In each folder you can keep:
- watchlist.txt - symbols used for that run
- pnl.json - entries and PnL snapshot for that run (baseline to compare after logic changes)

These files are committed. data/*.csv OHLC files are in .gitignore and not committed.
