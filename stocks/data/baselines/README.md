# Per-date baselines

One folder per date (YYYY-MM-DD). In each folder keep:
- watchlist.txt - symbols used for that run
- pnl.json - entries and PnL snapshot (baseline to compare after logic changes)

OHLC data in data/*.csv is gitignored; this folder is committed.
